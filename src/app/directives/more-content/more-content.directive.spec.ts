import { MoreContentDirective } from './more-content.directive';

describe('MoreContentDirective', () => {
  it('should create an instance', () => {
    const directive = new MoreContentDirective();
    expect(directive).toBeTruthy();
  });
});
